import CheckBox from '../../../@common/CheckBox/CheckBox';

const AllSelectCheckBox = () => {
  // hook을 통한 전체 선택 로직 추가 예정

  return <CheckBox isChecked={false} />;
};

export default AllSelectCheckBox;
